name: windows

on: workflow_call

env:
  MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE || 'ragnarok' }}
  MYSQL_USER: ${{ secrets.MYSQL_USER || 'ragnarok' }}
  MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD || 'ragnarok' }}
  MYSQL_ALLOW_EMPTY_PASSWORD: ${{ secrets.MYSQL_ALLOW_EMPTY_PASSWORD || 'yes' }}

jobs:
  build:
    runs-on: windows-2025
    timeout-minutes: 30
    strategy:
      matrix:
        include:
          - CC: '15.0'
            SDK: '10.0.15063.0'
          - CC: '16.0'
            SDK: '10.0.18362.0'
          - CC: '17.0'
            SDK: '10.0.19041.0'
          - CC: '17.0'
            SDK: '10.0.22000.0'
        RENEWAL: ["", "DISABLE_RENEWAL"]
        CLIENT_TYPE: ["", "ENABLE_PACKETVER_RE", "ENABLE_PACKETVER_ZERO"]
        PACKET_VERSION: ["PACKETVER=20221024", "PACKETVER=20130724"]
        exclude:
          - PACKET_VERSION: "PACKETVER=20130724"
            CLIENT_TYPE: "ENABLE_PACKETVER_ZERO"

    # github.head_ref will stop previous runs in the same PR (if in a PR)
    # github.run_id is a fallback when outside a PR (e.g. every merge in master will run, and previous won't stop)
    concurrency:
      group: msbuild-${{ github.head_ref || github.run_id }}_VisualStudio-${{ matrix.CC }}_SDK-${{ matrix.SDK }}_${{ matrix.RENEWAL }}_${{ matrix.CLIENT_TYPE }}_${{ matrix.PACKET_VERSION }}
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2
        with:
          vs-version: ${{ matrix.CC }}

      - name: Check WindowsTargetPlatformVersion
        run: |
          $version = (Get-ItemProperty "HKLM:\SOFTWARE\WOW6432Node\Microsoft\Microsoft SDKs\Windows\v10.0" -Name ProductVersion).ProductVersion
          Write-Host "WindowsTargetPlatformVersion: $version"

      - uses: shogo82148/actions-setup-mysql@v1
        with:
          distribution: "mariadb"
          mysql-version: "11.1"

      - name: init database
        run: |
          mysql -u root -e "CREATE DATABASE IF NOT EXISTS ${{ env.MYSQL_DATABASE }};"
          mysql -u root -e "CREATE USER IF NOT EXISTS '${{ env.MYSQL_USER }}'@'localhost' IDENTIFIED BY '${{ env.MYSQL_PASSWORD }}';"
          mysql -u root -e "GRANT SELECT,INSERT,UPDATE,DELETE ON ${{ env.MYSQL_DATABASE }}.* TO '${{ env.MYSQL_USER }}'@'localhost';"
          Get-Content sql-files/main.sql | mysql -u root ${{ env.MYSQL_DATABASE }}
          Get-Content sql-files/logs.sql | mysql -u root ${{ env.MYSQL_DATABASE }}

      - name: build
        run: |
          .\tools\ci\windows.ps1 build ${{ matrix.CC }} ${{ matrix.SDK }} ${{ matrix.CLIENT_TYPE }} ${{ matrix.PACKET_VERSION}} ${{ matrix.RENEWAL }}

      - name: run
        run: |
          .\tools\ci\windows.ps1 run

      - name: test
        if: matrix.PACKET_VERSION != 'PACKETVER=20130724'
        run: |
          .\tools\ci\windows.ps1 test
